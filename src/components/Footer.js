import React from 'react'

const Footer = (props) => {
    //Everything within the return is jsx and vars need {} for a jsx variable and expressions (.map, if-then, etc)
    return (
        <div id = 'footer'>Powered by Lee Macklin & React JS</div>
    )
}

export default Footer
